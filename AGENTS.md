# AIエージェント向け作業指針

## 対象範囲

- このリポジトリは PY32F071 を搭載する UV-K1 / UV-K5 V3 系を対象とする．旧UV-K5（DP32G030）向けのコード，メモリーマップ，ビルド手順を混ぜない．
- K1 と K5 V3 は同じファームウェア／CHIRPプロファイルとして扱う．機種固有の差分が判明した場合は，共通化したまま仮定せず，ソースと実機で確認してから分離する．
- 標準の利用目的は日本国内向けの受信専用運用である．送信経路，送信メニュー，送信を誘発する操作を再導入しない．PTTはモニター操作として維持する．

## 公開可能なプロジェクト前提

- このリポジトリはarmel系を上流とする派生フォークであり，公式Quansheng版，公式F4HWN版，armel版そのものではない．由来の異なるコードや資産を混在させない．
- UV-K1とUV-K5 V3は同じPY32F071系ファームウェア／CHIRPプロファイルとして扱う．旧UV-K5版とは，チップ，無線ドライバ，メモリーマップ，ビルド系統が異なるため，コードや手順を共有しない．
- 版番号は上流版番号に`J`と日本語版パッチ番号を続ける．受信専用版の出力名は`wrx-jp`系に統一し，署名や配布用パッケージはビルド検証と分離する．
- 受信専用はUI上の設定ではなく，ビルド，アプリケーション，無線制御の安全境界である．PTTはモニターに割り当て，予期しないTX要求には画面表示とビープで拒否を示す．
- `0xA1`–`0xDF`はJIS X 0201の半角カタカナ領域として扱い，ASCII領域やプロジェクト固有の拡張領域と混同しない．文字の意味・注釈はmanifestで管理し，フォントの編集は`tools/font_source_json.py`でC配列をコードポイント付きJSONへ抽出してから行う．JSONには基準バイト列を残し，適用時の不一致を拒否する．atlasと生成inventoryは派生物として更新する．
- 日本語フォントは固定セル内で設計し，欧文との行内位置を描画側の個別補正で合わせない．変更後はatlas画像を確認し，コードポイント，幅，高さ，未登録文字を検証する．
- フォント配列を行番号，正規表現の一致順，無効化された`#if 0`ブロックの位置で置換してはならない．JSONを抽出し，編集後は一時Cファイルへ適用して差分とatlasを確認する．`--in-place`は確認後だけ使う．
- `tools/font_editor.html`の文字列プレビューは字形を縮小せず表示幅に応じて改行する．「日本語例」はJIS X 0201相当の確認用文字列を設定し，JSONパッチは台帳読込後に再読込できる．
- 外部資料や他プロジェクトの実装を参照する場合は，ライセンスと出所を確認し，互換性のない資産やコードをそのまま取り込まない．仕様・挙動を独立に実装した場合は，必要な謝辞とライセンス表示を保つ．

## 変更時の不変条件

- `JpRxOnly`（`CMakePresets.json`）を日本語・受信専用版の正規ビルド入口とする．出力名は `wrx-jp` 系を使用し，上流版と混同する名前を増やさない．
- 日本語表示を変更するときは，大文字／小文字表示，表示幅，長音「ー」などのフォント境界を確認する．文字列を短くする場合も，受信機としての意味が変わらないことを確認する．
- `RXExt` は追加受信機能だけをまとめて切り替える．受信専用，PTTモニター，日本語表示，国内FM帯域の制約をこの設定で解除しない．
- K1/K5 V3 の標準設定・チャンネル名・校正領域と，受信拡張の外部フラッシュ領域を混同しない．新しい永続データを追加するときは，既存設定・校正・復旧手順への影響を明記する．
- calibration は利用者設定ではない．通常のCHIRPアップロード範囲に校正領域を含めず，書き込み範囲を拡張する場合はバックアップ，復元，境界テストを先に用意する．低レベルUARTや保守用経路まで絶対保護されるとは仮定しない．
- 実機未検証の機能は，README・テスト結果・リリース説明で未検証と明示する．静的テストやホストテストを実機動作の代わりに扱わない．

## AIエージェントの作業手順

- 作業開始時に未コミット変更，対象ソース，テスト，適用される指示を確認する．既存の変更をリセット・上書きしない．
- 機能変更では，影響範囲を確認してから実装し，対応するホストテスト，[DEVELOPMENT.md](DEVELOPMENT.md)，[docs/FEATURES_TECHNICAL.ja.md](docs/FEATURES_TECHNICAL.ja.md)を必要な範囲で更新する．
- 利用者向け文書は[README.md](README.md)，[README.ja.md](README.ja.md)，[CHEATSHEET.ja.md](CHEATSHEET.ja.md)の役割を混ぜない．詳細な開発情報は`DEVELOPMENT.md`へ置く．
- 実機未検証の結果を，ビルド成功やホストテスト成功だけで保証済みと記載しない．
- 変更後は，可能な範囲でテスト，ビルド，`git diff --check`を実行し，未検証範囲を報告する．具体的なコマンドと出力物は`DEVELOPMENT.md`を参照する．

## 変更・履歴の扱い

- 変更前に既存の未コミット変更を確認し，意図しないフォーマット変更や生成物を取り込まない．
- 機能変更，メモリーマップ変更，表示文言変更は，対応するテストと正規文書を同じ変更単位で更新する．
- コミット，タグ，署名，リリース用バイナリの作成は，ユーザーが明示的に依頼した場合だけ行う．署名が必要な場合は，署名者の手順と責任範囲を変更しない．

