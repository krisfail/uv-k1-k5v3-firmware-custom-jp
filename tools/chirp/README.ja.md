# wrx-jp CHIRPドライバ

`wrx_jp.py`は，K1とK5 V3を同じPY32F071プロファイルで扱い，旧UV-K5だけをDP32G030プロファイルで扱う1ファイル構成です．

| CHIRPで選ぶ機種 | 対象ハードウェア |
| --- | --- |
| `UV-K1 / UV-K5 V3 (wrx-jp RX-only)` | UV-K1，UV-K5 V3 |
| `UV-K5 (wrx-jp RX-only)` | 旧UV-K5（DP32G030） |

CHIRPのドライバ配置先へ`wrx_jp.py`をコピーして読み込んでください．機種選択を誤るとメモリーマップが一致しないため，アップロード前に対象機種から全イメージをダウンロードして保存します．

このドライバは受信専用です．CHIRP上では送信周波数，送信トーン，PTT，出力設定を扱いません．FMプリセットは日本向けに`76.0–95.0 MHz`として読み書きします．FM登録数はK1/K5 V3が48件，旧K5が20件です．K1/K5 V3の追加RX機能状態と校正値は，アップロード対象から除外しています．

## 校正領域の保護

CHIRPのアップロードは，通常のチャンネル・VFO・設定だけを対象にした許可リスト方式です．K1／K5 V3では論理アドレス`0xB000–0xB1FF`の校正領域を含めず，旧UV-K5では`0x1E00–0x1FFF`付近の校正領域を含めません．全イメージのダウンロードには個体固有のデータが含まれ得るため，アップロード前のバックアップとして保存してください．

これはCHIRPのアップロード経路に対する保護です．UARTや保守用ツールなど別の低レベル書き込み経路まで，校正領域が絶対に書き込めないことを意味しません．校正データを変更する操作は行わず，復元が必要な場合だけ個体のバックアップから明示的に復元してください．書き込み範囲を変更するコードでは，境界を含むテストと復旧手順を先に更新します．

実機でのCHIRP読み書きは未検証です．バックアップを保持したうえで，まず読み取りだけを確認してください．

## K1／K5 V3の日本語チャンネル名

K1／K5 V3では、通常の16 byte ASCII名slotを互換用に残し、日本語名を外部Flashの固定テーブルへ保存します。テーブルは外部物理アドレス0x040000から1024件×32 byteです。1件の入力はUTF-8で最大31 byte、表示幅はASCIIを8 pixel、日本語glyphを16 pixelとして検査します。

0x020000から配置する固定フォントデータはIzumi 16です。CHIRPはアップロード開始時にフォントデータ全体を読み出して照合し、不一致なら128 byte単位で書き込み、各ブロックをreadback確認します。失敗時は同じブロックを最大3回再試行します。名前テーブルも同じ32 bit外部Flashコマンドで書き込みます。

ドライバをCHIRPのドライバディレクトリへ単独コピーする場合は、生成済みのjapanese_font.binとjapanese_font_manifest.jsonも同じディレクトリへ置きます。リポジトリ内で実行する場合は、docs/fontsとtoolsの生成物を自動検出します。

外部コマンドはこのドライバとJpRxOnlyファームウェアのversion付き契約です。既存の16 bit EEPROMコマンドは変更せず、外部読出し0x0531／応答0x0532、外部書込み0x0533／応答0x0534を追加しています。校正領域や任意アドレスはファームウェア側の範囲検査で拒否します。

旧UV-K5（PY32F071ではないDP32G030プロファイル）は従来どおり10文字ASCII名です。無線機上の日本語入力、スクロール、A/B二重化、journalはこの段階では実装していません。

帰属・ライセンスは同じディレクトリの`NOTICE.md`と`LICENSE.txt`を参照してください．

